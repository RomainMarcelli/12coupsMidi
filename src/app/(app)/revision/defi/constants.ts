/**
 * H3 — Constantes du défi du jour. Extraites de `actions.ts` pour
 * pouvoir être importées dans des contextes non-server (route cron
 * Vercel notamment). Une "use server" file ne peut exporter que des
 * fonctions async.
 */
export const DAILY_CHALLENGE_QUESTION_COUNT = 10;
